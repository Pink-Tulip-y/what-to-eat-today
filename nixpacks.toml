# Railway 部署配置
[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = ["npm install", "cd server && npm install", "cd client && npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
